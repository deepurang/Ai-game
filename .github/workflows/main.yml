name: Build AI Quiz Battle APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Extract project
        run: |
          unzip -q AI_Quiz_Battle_V5_APK_Ready.zip
          find AI_Quiz_Battle_V5_APK_Ready -maxdepth 3 -type f

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build APK
        run: |
          cd AI_Quiz_Battle_V5_APK_Ready
          gradle assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: AI-Quiz-Battle-APK
          path: AI_Quiz_Battle_V5_APK_Ready/app/build/outputs/apk/debug/*.apk
